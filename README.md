# FizzBuzz

A simple script that takes user input and returns the FizzBuzz result.

## How it works

- Enter a number.
- The script returns:
  - **Fizz** if divisible by 3  
  - **Buzz** if divisible by 5  
  - **FizzBuzz** if divisible by both  
  - The number itself otherwise